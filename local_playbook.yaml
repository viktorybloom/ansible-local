- hosts: localhost
  vars:
    source_key: "./.ssh/id_rsa"
    dest_key: "{{ lookup('env', 'HOME') }}/.ssh/id_rsa"
    personal: "{{ lookup('env', 'HOME') }}/personal"
  pre_tasks:
    - name: Update Pacman
      become: true
      pacman:
        update_cache: yes
        state: present
      tags:
        - install
        - core
  tasks: 
#    - include_tasks: tasks/ssh.yaml
    - include_tasks: tasks/productivity.yaml
    - include_tasks: tasks/core.yaml
    - include_tasks: tasks/git-setup.yaml
      
    # docker playbook not running - investigate later.
    # in the meantime run - https://docs.docker.com/engine/install/linux-postinstall/
    # - include: playbook/docker.yaml

